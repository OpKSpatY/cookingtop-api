/**
 * Exemplo resumido de `GET /recipes/pantry-availability`
 * (cada lista contém o mesmo formato de receita que `GET /recipes`).
 */
export const recipePantryAvailabilityExample = {
  can_make: [],
  cannot_make: [],
} as const;
